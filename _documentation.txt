------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
НЕАКТУАЛЬНО!!! убраны статические конструкторы!!!

рекурсивные зависимости выдадут некорректные результаты из-за спецификации языка C#:

It is possible for static fields with variable initializers to be observed in their default value state. However, this is strongly discouraged as a matter of style. The example
using System;
class Test
{
	static int a = b + 1;
	static int b = a + 1;
	static void Main() {
		Console.WriteLine("a = {0}, b = {1}", a, b);
	}
}
exhibits this behavior. Despite the circular definitions of a and b, the program is valid. It results in the output
a = 1, b = 2
because the static fields a and b are initialized to 0 (the default value for int) before their initializers are executed. When the initializer for a runs, the value of b is zero, and so a is initialized to 1. When the initializer for b runs, the value of a is already 1, and so b is initialized to 2.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

сделан transient scope
описать в документации!

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

написать тесты на циклы в графе; в том числе с conditional биндингами
подумать насчет возможности сделать тесты вероятностными (то есть искать не все варианты, а ограниченные по времени), или даже полностью отключать их; это бы сэкономило время компиляции
подумать насчет пропускать подграфы, например с помощью атрибута или внутри кляузы Bind а-ля Bind<>().To<>().SkipCirculalCheck()

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

make Debug note if singleton takes transient as its dependency
сделано! описать в документации

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

вчерне набросан, описать в документации
make conditional bindings

также в документации описать влияние на производительность; без кондишенов - на порядок быстрее; и лучше бы условные биндинги иметь наверху цепочки зависимостей,
так быстрее работает


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

make GetAll<> and GetAll(typeof())

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

make bind to constant

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

описать в документации:
не разрешается диспоузить модули во время резолвинга! это может привести к неопределенным последствиям

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

тесты что объект не создается, если он не должен создаваться (например изза кондишионал биндинга, или есть несколько IA, когда IB потребляет IA - в таком случае кидается
исключение TooManyBindings а IA не должны резолвиться)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

сделать тест где B потребляет два IA; посмотреть что все компилится ОК и что в B попадают те объекты, которые требуются
вероятно IA придется биндить с условием When

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

тесты на простые типы а-ля int string

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

тесты на сложные типы, а ля дженерики (можно посмотреть в репу ProxyGenerator, там много заковык придумано на эту тему)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

убрать использование короткого имени типа отовсюду. даже саму пропертю убрать

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

попробовать unchecked оптимизации

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

только один инстанс модуля может быть в домене, так как там всё завязано на статики

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
